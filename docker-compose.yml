services:
  app:
    image: node:20-alpine
    working_dir: /app
    ports:
      - "8081:8081"
    volumes:
      - ./src:/app
    environment:
      - NODE_ENV=development
    command: sh -c "npm run dev -- --host"
